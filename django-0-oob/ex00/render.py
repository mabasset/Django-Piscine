import sys, os, re

def my_render(template_file):
	

if __name__ == "__main__":
	if len(sys.argv) != 2 or not argv[1].endswith(".template")
		sys.exit(1)
	try:
		with open(argv[1], 'r') as f:
			my_render(f)
	except:
		sys.exit(1)